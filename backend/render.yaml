services:
  - type: web
    name: attendance-backend
    runtime: node
    rootDir: .
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        sync: false
      - key: ADMIN_PINS
        sync: false
      - key: PORT
        value: 5000
